# Copy to .env.local — the docker-compose Neo4j from docker-compose.yml.
# This is the default target: commands with no --env and no MED_GRAPH_ENV
# come here, so a forgotten flag cannot reach production.
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=
