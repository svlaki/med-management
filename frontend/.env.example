# Base URL of the FastAPI backend, no trailing slash.
# Leave unset for local development: Vite proxies /api to localhost:8000.
# Set in Vercel to the Railway public URL, e.g.
#   VITE_API_BASE_URL=https://med-graph-api-production.up.railway.app
VITE_API_BASE_URL=
